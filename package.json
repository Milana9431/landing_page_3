{
  "name": "landing-page-3",
  "version": "1.0.0",
  "description": "landing page",
  "source": "index.html",
  "scripts": {
    "sass:watch": "sass --style=expanded --watch scss:dist",
    "parcel:watch":"parcel serve --dist-dir public",
    "parcel:build":" parcel build --dist-dir build --public-url ./"
  },
  "author": "Milana",
  "license": "ISC",
  "dependencies": {
    "parsel": "^0.3.0",
    "sass": "^1.78.0"
  }
}
